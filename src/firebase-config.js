import { initializeApp } from 'firebase/app';

// Your web app's Firebase configuration
const firebaseConfig = {
  //todo: place your firbase config here
};

export const app = initializeApp(firebaseConfig);
